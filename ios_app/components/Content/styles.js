import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
	container: {
		backgroundColor: 'red',
		height: '100%',
		width: 370
	},

	text: {
		fontSize: 100
	}
});


export default styles;